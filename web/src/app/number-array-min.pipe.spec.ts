import { NumberArrayMinPipe } from './number-array-min.pipe';

describe('NumberArrayMaxPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberArrayMinPipe();
    expect(pipe).toBeTruthy();
  });
});
